const MfaVerifyInstructions = () => {
  return (
    <p className="text-slate-500 font-semibold mt-2">
      Using your authenticator app, enter the 6 digit code that is shown in the
      app, before it expires.
    </p>
  );
};

export default MfaVerifyInstructions;
